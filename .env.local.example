# =============================================================================
# Required Environment Variables
# =============================================================================

# Next.js Public App URL
# This is the base URL where your app will be deployed
NEXT_PUBLIC_APP_URL=https://your-domain.com

# Supabase Configuration
# Get these from your Supabase project settings
SUPABASE_URL=https://xxx.supabase.co
SUPABASE_KEY=your-service-role-key

# =============================================================================
# Optional Environment Variables
# =============================================================================

# Allowed Embed Origins
# Comma-separated list of domains allowed to embed the waitlist iframe
# If not set, only NEXT_PUBLIC_APP_URL will be allowed
ALLOWED_EMBED_ORIGINS=https://yoursite.com,https://marketing.yoursite.com

# Additional Allowed Origins (for CSRF protection)
# Comma-separated list of additional origins allowed to make API requests
# ALLOWED_ORIGINS=https://another-domain.com

# =============================================================================
# Email Configuration (Optional)
# =============================================================================
# If these are not set, email confirmations will not be sent
# The app will still function normally without email

# SMTP Server Configuration
SMTP_HOST=smtp.zoho.com
SMTP_PORT=465

# SMTP Credentials
SMTP_USER=your-email@domain.com
SMTP_PASS=your-password

# From Email Address (defaults to SMTP_USER if not set)
SMTP_FROM=noreply@domain.com
